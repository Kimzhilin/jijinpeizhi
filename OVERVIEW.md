# 偏股基金平衡配置 · 调仓助手 — 交付概览

## 做了什么
把你之前确认的「稳健偏防御型偏股配置 + 20% 现金机动 + 跌补涨减再平衡」逻辑，做成一个**纯静态、可部署到 GitHub Pages** 的网页工具（无服务器 / 无数据库 / 无密钥，数据存浏览器本地）。

## 交付文件（`fund-allocator/` 目录）
- `index.html` — 页面结构（配置 / 持仓行情 / 调仓建议 / 交易记录 四个分区）
- `styles.css` — 浅色主题、卡片式、移动端自适应
- `app.js` — 全部逻辑（状态管理、净值获取、盈亏计算、调仓建议引擎、本地存储）
- `README.md` — GitHub Pages 部署步骤 + 数据说明 + 免责声明

## 核心能力
1. 输入可投金额 → 自动生成 5 只基金目标金额 + 20% 现金。
2. 记录买入 / 卖出 → 实时更新持仓市值与盈亏。
3. 刷新行情 → 拉东方财富最新官方净值 + 当日涨跌，重算盈亏。
4. 调仓建议引擎（按纪律自动生成可执行的买/卖）：
   - 偏离目标 ±5% 再平衡；
   - 成长 +20% / +35% 分批止盈入现金；
   - 近 3 月高点回撤：宽基/红利 -8/-15/-25、成长 -10/-20/-30 分档加仓；
   - 现金维持 5%–25%；单日波动 ≤3% 不主动操作。
5. 交易记录留痕 + 一键清空。

## 关键技术决策
- **数据源变更**：原计划用 `fundgz` 盘中估值接口，实测已失效（404）。改用东方财富 `pingzhongdata` 历史净值接口（已验证 5 只基金均可返回），以「最新官方净值 + 当日涨跌 + 近 3 月高点」为数据源。验证结果示例：沪深300 回撤 -8.0%、科创50 -24.6%、创业板 -22.1%、红利低波 -0.4%，建议引擎触发逻辑符合预期。
- 联接基金净值每日收盘后更新一次，故工具显示「最新净值」而非分时跳动；盘中无可靠免费估算接口，已在 UI 与 README 中如实说明。
- 接口不可用时支持手动填净值，不影响记账。

## 使用方式
- 本地：双击 `index.html` 即可试用。
- 部署：仓库根目录上传 4 个文件 → Settings → Pages → Deploy from a branch → 访问 `https://<用户名>.github.io/fund-allocator/`。

## 边界
纯静态站点无后台，页面关闭期间不自动更新（打开 / 点刷新才拉数据；交易时段每 5 分钟自动刷）。如需无人值守每日自动跑，需加 GitHub Actions 定时任务（可另议）。本工具为个人记账 / 辅助决策用，不构成投资建议。
